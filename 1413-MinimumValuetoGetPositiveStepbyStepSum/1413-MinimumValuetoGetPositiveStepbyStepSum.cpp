        while(low<=high) {
            int mid = high - (high-low)/2;
            if(isValid(nums, mid)) {
        int low = 1, high = max_num;
        max_num++;
        max_num *= n;
        }
            max_num = max(max_num, abs(x));
        for(int x:nums) {
        int max_num = INT_MIN;
[
