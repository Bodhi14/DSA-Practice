subarray and even[i-1] for the curr number taken with the prev odd sum subarray
                CurrOdd = PrevEven + 1;// +1 for the odd number separately being a 
            if(arr[i]%2) {
        for(int i = 0; i<n; ++i) {
    int numOfSubarrays(vector<int>& arr) {
        int n = (int)arr.size(), ans = 0, CurrOdd = 0, CurrEven = 0, PrevEven = 0, 
PrevOdd = 0;
                CurrEven = PrevOdd;
            }
[
