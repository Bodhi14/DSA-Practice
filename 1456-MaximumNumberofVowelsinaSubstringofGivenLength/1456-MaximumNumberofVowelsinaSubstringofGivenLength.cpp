            if(isVowel(curr)) currSum++;
        }
        maxSum = currSum;
        for(int i = 1; (i+k-1)<n; ++i) {
            char curr = s[i];
        for(int i = 0; i<k; ++i) {
        int currSum = 0, maxSum;
        int n = (int)s.size();
            if(isVowel(s[i-1])) currSum--;
            if(isVowel(s[i+k-1])) currSum++;
"
