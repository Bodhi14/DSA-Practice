            int curr;
            if(abs(nums[low])>abs(nums[high])) {
                curr = nums[low];
            }
                low++;
        for(int i = 0; i<n; ++i){
        
        int low = 0, high =n-1;
        vector<int> ans(n);
        if(n==1) return {nums[0]*nums[0]};
        int n = (int)nums.size();
            else {
                curr = nums[high];
                high--;
            }
[
