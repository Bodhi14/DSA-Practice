            for(int j = 0; j<m; ++j) {
                if(matrix[i][j] == rowMin[i] && matrix[i][j] == colMax[j]) {
        for(int i = 0; i<n; ++i) {
        }
            colMax[i] = max_num;
                    ans.push_back(matrix[i][j]);
                }
            }
                    break;
        }
        return ans;
    }
[
