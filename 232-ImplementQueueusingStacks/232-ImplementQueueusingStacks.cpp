            }
                st1.pop();
                st2.push(st1.top());
        else {
            while(!st1.empty()) {
    }
    
    void push(int x) {
        if(st1.empty()) st1.push(x);

[
