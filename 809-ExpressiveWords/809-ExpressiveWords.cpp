            int k = 0;
            bool flag = true;
            while (j < s.length() && flag)
            {
    int expressiveWords(string s, vector<string>& words) {
        int count = 0;
        for (auto w : words)
        {
            if (w.length() > s.length())
                continue;
            int i = 0;
            int j = 0;
public:
class Solution {
"
