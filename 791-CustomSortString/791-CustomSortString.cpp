            }
                }
                    if(mp[ch]==0) mp.erase(ch);
                    mp[ch]--;
                    ans+=ch;
                while(mp.count(ch)) {
                vis[ch]++;
            if(mp.find(ch)!=mp.end()) {
        for(char& ch: order) {
        for(char& ch: s) mp[ch]++;
        }
"
