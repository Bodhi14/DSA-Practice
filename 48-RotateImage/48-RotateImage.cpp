        for(int i = 0; i<n; ++i) {
            reverse(matrix[i].begin(), matrix[i].end());
        }
        return;
    }
};
        }
            }
                swap(matrix[i][j], matrix[j][i]);
            for(int j = i; j<n; ++j) {
[
