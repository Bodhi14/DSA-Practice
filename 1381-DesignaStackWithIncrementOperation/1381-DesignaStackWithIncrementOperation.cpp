        if(stk.size() < maxsize ) stk.push_back(x);
    void push(int x) {
    
    CustomStack(int maxSize) {
        maxsize = maxSize;
    }
    int maxsize;
    deque<int> stk;
public:
class CustomStack {
[
