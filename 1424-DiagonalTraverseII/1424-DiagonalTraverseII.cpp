            for(int j = 0; j<m; ++j) {
        for(int i = 0 ; i<n; ++i) {
                mp[i+j].push_back(nums[i][j]);
            }
        map<int, vector<int>> mp;
            int m = (int)nums[i].size();
        }
        for(auto it:mp) {
            for(int x:it.second) ans.push_back(x);
            reverse(it.second.begin(), it.second.end());
            ve
[
