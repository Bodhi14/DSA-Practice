            end = end->next;
        }
        while(b>-1) {
        }
        ListNode* start = list1, *end = list1;
        while(a>1) {
            start = start->next;
            a--;
    ListNode* mergeInBetween(ListNode* list1, int a, int b, ListNode* list2) {
class Solution {
public:
        start->next = list2;
        while(list2->next) {
            b--;
[
