class Solution {
public:
    int majorityElement(vector<int>& nums) {
        sort(nums.begin(), nums.end());
    }
        return nums[midEle];
        int n = (int)nums.size();
        int midEle = n/2;
        if(!(n%2)) midEle--;
};
[
