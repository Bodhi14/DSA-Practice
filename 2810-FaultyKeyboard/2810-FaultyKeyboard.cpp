        }
            }
        return s;
               i--;
               s.erase(s.begin()+i);
               reverse(s.begin(), s.begin()+i);
            if(s[i]=='i') {
        for(int i=0;i<s.size();++i) {
    }
};
"
