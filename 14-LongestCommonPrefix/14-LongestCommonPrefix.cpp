        for(auto str: strs) {
            int ss = (int)str.size();
            if(min_size > ss) {
                min_size = ss;
    string longestCommonPrefix(vector<string>& strs) {
        int n = strs.size();
        int min_size = INT_MAX;
        string min_str, ans;
class Solution {
public:
[
